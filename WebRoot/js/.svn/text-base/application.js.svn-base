// 这是一个管理着 视图/控制/模型 的全局类
var App = {
	Models: {},
	Views: {},
};

var App = {
	Models: {},
	Views: {},
	Routers: {},
	Collections: {},
	data:{},
	
	initialize: function() {
		this.data.mainRouter = new App.Routers.HMapVis();
		new App.Routers.HMapVis();
		Backbone.history.start(); // 要驱动所有的Backbone程序，Backbone.history.start()是必须的。
	}
	
};